const BASE = "/";
const ABOUT = BASE + "about";
const LOGIN = BASE + "login";
const SIGNUP = BASE + "signup";
const SETTINGS = BASE + "settings";
const PASSWORD_RESET = BASE + "reset-password";



const AppRoutes = {
	BASE,
	ABOUT,
	LOGIN,
	SIGNUP,
	SETTINGS,
	PASSWORD_RESET,

};

export default AppRoutes;
